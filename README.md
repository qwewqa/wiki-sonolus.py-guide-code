# Sonolus.py Guide Code

This project is the runnable companion to the Sonolus.py guide.

## Setup

1. Install [uv](https://docs.astral.sh/uv/).
2. Run `uv sync`.
3. Run `uv run sonolus-py dev` to start the development server.

Build the engine with `uv run sonolus-py build`.
